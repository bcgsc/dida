#ifndef MERGE_H
#define MERGE_H
/*
 * Tony's requested version
 * for Q=0 for same CIGAR
 */

#include <iostream>
#include <fstream>
#include <sstream>
#include <string>


void getInf(unsigned &, unsigned &);

int getMerge(const int);

#endif
